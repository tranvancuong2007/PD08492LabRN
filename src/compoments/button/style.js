import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: 'orange',
        paddingVertical: 20,
        paddingHorizontal: 8,
        borderRadius: 8,
        marginTop: 20,
    },

    textContent: {
        color: 'white',
        textAlign: 'center',
        fontSize: 16,
        fontWeight: 'bold',
    },
});