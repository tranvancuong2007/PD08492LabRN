import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
    },

    iconBack: {
        width: 18,
        height: 18,
        marginTop: 10,
        marginBottom: 20,
    },

    title: {
        color: 'blue',
        fontSize: 26,
        fontWeight: '500',
        paddingHorizontal: 10,
        marginTop: 10,
        marginBottom: 20,
    },
});