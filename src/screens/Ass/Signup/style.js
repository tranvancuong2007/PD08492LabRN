import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 24,
    },

    checkRow: {
        flexDirection: 'row',
        alignItems: 'center',
        marginBottom: 30,
    },

    checkText: {
        color: 'red',
        marginHorizontal: 13,
    },

    button: {
        marginVertical: 20,
    },

    footerText: {
        color: 'red',
        marginBottom: 56,
        textAlign: 'center',
    },
 
    footerLink: {
        fontWeight: 'bold',
    },
});