import {View, Text} from 'react-native';

const User = () => {
    return (
        <View style = {{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
            <Text style = {{color: 'blue', fontSize: 40}}>User Screen</Text>
        </View>
    );
};

export default User;